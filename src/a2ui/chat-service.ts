/**
 * Legacy A2UI chat-service facade.
 *
 * OpenUI Lang owns chat decoration, action handling, history, idempotency, and
 * pipe logging. The old service class name is kept as an alias for compatibility.
 */

export { OpenUILangChatService as A2UIChatService } from "../openui-lang/module.js";
