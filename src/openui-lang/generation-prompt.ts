import { BASIC_OPENUI_COMPONENT_CONTRACT_DOCS, OPENUI_BUSINESS_COMPONENT_CONTRACT_DOCS, formatOpenUIComponentContractDocs } from "./component-contracts.js";
import { OPENUI_LANG_BASIC_CATALOG_ID, OPENUI_LANG_BASIC_CATALOG_VERSION, OPENUI_LANG_VIEW_PROTOCOL } from "./types.js";

export function createOpenUILangGenerationPrompt(): string {
  return [
    "OpenUI Lang structured rendering rules:",
    "",
    "Default path: keep structured tool output intact; OpenUI Presentation Policy will automatically decide whether to render a structured surface.",
    "Do not explicitly choose a UI component when rows/groups/aggregates/metrics/charts/insights/sources can describe the answer shape.",
    "Use openui.lang.delegate only as an advanced explicit channel when the model must author a custom OpenUI Lang document.",
    "a2ui.delegate is a backward-compatible alias only; prefer openui.lang.delegate.",
    "The decision is presentation-driven, not domain-name-driven: do not require an inventory/leave/approval-specific surface before using OpenUI Lang.",
    "Let Presentation Policy decide from the output shape: rows, grouped numeric aggregates, metrics, charts, insights, risks, forms, actions, timelines, or citations are strong OpenUI candidates.",
    "When the final answer needs a table/chart/card/list/dashboard, preserve structured.rows, structured.groups, structured.aggregates, structured.metrics, structured.charts, structured.insights, and structured.sources instead of flattening everything into Markdown.",
    "For charts, preserve the fields that make the chart inferable: time fields for trends, category fields for distributions/rankings/composition, and numeric value fields for measures.",
    "For external search or research-style answers, preserve sources[] and insights[]; use rows[] when there are comparable items or detailed records.",
    "Do not call openui.lang.delegate for a simple one- or two-sentence answer.",
    "If openui.lang.delegate is called, do not add text before or after the tool call; the OpenUI Lang renderer owns the final display.",
    "",
    "Current project protocol:",
    "- Internal protocol: OpenUI Lang openui-lang/1.0, using componentName + props + children as the canonical schema.",
    `- Primary catalog: ${OPENUI_LANG_BASIC_CATALOG_ID}, catalog_version=${OPENUI_LANG_BASIC_CATALOG_VERSION}.`,
    "- Legacy A2UI v0.9 envelopes are compatibility transport only; do not design new renderer behavior around A2UI.",
    "- Renderer consumes OpenUI Lang surfaces adapted to createSurface + updateDataModel + updateComponents and safely falls back for unsupported components.",
    "- Legacy Basic Catalog shape { id, component: { ComponentName: props } } is only a compatibility adapter.",
    `- OpenUI bridge protocol: ${OPENUI_LANG_VIEW_PROTOCOL}. Use it only for registered business surfaces.`,
    "",
    "Supported Basic Catalog components:",
    ...formatOpenUIComponentContractDocs(BASIC_OPENUI_COMPONENT_CONTRACT_DOCS),
    "",
    "Supported OpenUI business components:",
    ...formatOpenUIComponentContractDocs(OPENUI_BUSINESS_COMPONENT_CONTRACT_DOCS),
    "- Domain renderers may add LeaveRequestForm, DealerVehicleProgress, and other registered business components at runtime.",
    "",
    "Layout selection:",
    "- Generic surface mapping is automatic: rows -> DataTableSurface, metrics/aggregates -> MetricCardsSurface, charts.kind=bar -> BarChartSurface, charts.kind=pie -> PieChartSurface, charts.kind=line -> LineChartSurface, metrics+charts+rows/insights -> AnalyticsDashboardSurface, insights -> InsightSummarySurface, risk rows -> RiskListSurface, grouping hint -> GroupedListSurface, sources -> CitationDisclosure.",
    "- Use generic OpenUI components first when the data shape is generic: DataTableSurface, GroupedListSurface, RiskListSurface, MetricCardsSurface, BarChartSurface, PieChartSurface, LineChartSurface, InsightSummarySurface, AnalyticsDashboardSurface, EvidenceSurface, TaskTrackingSurface, PendingActionSurface.",
    "- Use domain-specific components only when a registered renderer adds real extra value; otherwise stay with generic surfaces.",
    "- Three or more rows of same-type field comparison should be shown as a table-like/list surface, not many horizontal cards.",
    "- Two independent metrics can be shown as a Row of compact Cards.",
    "- Three to five same-size independent cards can be shown as Row with wrapping semantics in the renderer.",
    "- Single detail block or narrative content should be a vertical Card.",
    "- Tables/lists should avoid nested decorative Cards when the renderer already provides a boundary.",
    "- Text is for natural language and metrics. Short state labels can be modeled as Text/Icon or OpenUI tags when the business renderer supports them.",
    "",
    "Forms:",
    "- Form-like surfaces should use an OpenUI component with props.form.",
    "- props.form fields use { name, label, component, required, options }, initialValues, and submitAction.",
    "- submitAction.name must be a registered OpenUI action, normally openui.form.submit. a2ui.form.submit is a legacy alias only.",
    "- Never put arbitrary JavaScript functions or expressions in schema; schema is sanitized before render.",
    "",
    "Actions:",
    "- Buttons must use action.event.name and context. Do not expose internal tool names directly to the client.",
    "- Supported actions are advertised by /api/openui/capabilities and enforced by the action registry.",
    "",
    "Data binding:",
    "- Prefer updateDataModel for data, and Text.path JSON Pointer such as /user/name when rendering values.",
    "- JSON Pointer escaping is supported: ~1 for slash and ~0 for tilde.",
    "",
    "Streaming:",
    "- Surface shell should be creatable before all data is known: createSurface first, then updateDataModel/updateComponents.",
    "- Prefer stable component ids so incremental updates and history replay stay consistent.",
    "",
    "Final verification:",
    "- Every root id must exist in updateComponents.",
    "- Every child id referenced by a component must be defined.",
    "- Every componentName must be in Basic Catalog or carried through a whitelisted OpenUI renderer.",
    "- Unsupported components must degrade to safe readable text rather than causing a blank UI."
  ].join("\n");
}
